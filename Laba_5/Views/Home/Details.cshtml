@model Laba_5.Models.User
@{
    ViewBag.Title = Model.Name;
}
<h2>Пользователь @Model.Name</h2>
<div>
    <dl class="dl-horizontal">
        <dt>Идентификатор</dt>
        <dd>@Model.Id</dd>
 
        <dt>Возраст</dt>
        <dd>@Model.Age</dd>

        <dt>Информация о заказе</dt>
        <a asp-action="Order_Details" asp-controller="Home" asp-route-id="@Model.Id" >Подробнее</a> 
    </dl>
</div>